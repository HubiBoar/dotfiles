mkdir -p ~/projects;
mkdir -p ~/.docker;
mkdir -p ~/.password-store;
mkdir -p ~/.gnupg;

mkdir -p ./profiles/${PROFILE}/state;
mkdir -p ./profiles/${PROFILE}/state/root;
touch ./profiles/${PROFILE}/lazy-lock.json;
